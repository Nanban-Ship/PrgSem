<Window x:Class="TODOlist.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TODOlist.ViewModel"
        Title="MVVM TODO List" Height="450" Width="450">

    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox Text="{Binding NewTaskText, UpdateSourceTrigger=PropertyChanged}" Width="180" Margin="0,0,10,0"/>

            <DatePicker SelectedDate="{Binding NewTaskDeadline}" Width="110" Margin="0,0,10,0"/>

            <Button Content="Add Task" Command="{Binding AddCommand}" Width="70"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="Task List:" FontWeight="SemiBold" Margin="0,0,0,5"/>

        <ListBox Grid.Row="2" ItemsSource="{Binding Tasks}" SelectedItem="{Binding SelectedTask}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="0,3">
                        <TextBlock x:Name="TxtTask" Text="{Binding Text}" Margin="0,0,15,0" FontWeight="Medium"/>
                        <TextBlock x:Name="TxtDate" Text="{Binding Deadline, StringFormat=d}"/>
                    </StackPanel>

                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsOverdue}" Value="True">
                            <Setter TargetName="TxtTask" Property="Foreground" Value="Red" />
                            <Setter TargetName="TxtDate" Property="Foreground" Value="Red" />
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button Grid.Row="3" Content="Remove Selected Task" Command="{Binding RemoveCommand}" Margin="0,15,0,0" Height="30"/>
    </Grid>
</Window>